-- Gui to Lua
-- Version: 3.2

-- Instances:

local _7083G2222 = Instance.new("ScreenGui")
local MainFrame = Instance.new("ScrollingFrame")
local UIListLayout = Instance.new("UIListLayout")
local SecondaryFrame = Instance.new("Frame")
local Templatebbc = Instance.new("TextLabel")
local Folder = Instance.new("Folder")
local Template = Instance.new("TextButton")
local Viewing = Instance.new("BoolValue")

--Properties:

_7083G2222.Name = "7083G2222"
_7083G2222.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
_7083G2222.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

MainFrame.Name = "MainFrame"
MainFrame.Parent = _7083G2222
MainFrame.Active = true
MainFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
MainFrame.BackgroundTransparency = 0.500
MainFrame.BorderColor3 = Color3.fromRGB(98, 98, 98)
MainFrame.BorderSizePixel = 5
MainFrame.Position = UDim2.new(0.669195056, -65, 0.694620252, -86)
MainFrame.Size = UDim2.new(0, 414, 0, 231)

UIListLayout.Parent = MainFrame
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 5)

SecondaryFrame.Name = "SecondaryFrame"
SecondaryFrame.Parent = _7083G2222
SecondaryFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
SecondaryFrame.BackgroundTransparency = 1.000
SecondaryFrame.Position = UDim2.new(0.241037369, 0, 0.179012343, 0)
SecondaryFrame.Size = UDim2.new(0, 718, 0, 100)

Templatebbc.Name = "Templatebbc"
Templatebbc.Parent = _7083G2222
Templatebbc.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Templatebbc.BackgroundTransparency = 1.000
Templatebbc.Position = UDim2.new(0.129346311, 0, 0.310000002, 0)
Templatebbc.Size = UDim2.new(0, 588, 0, 50)
Templatebbc.Visible = false
Templatebbc.Font = Enum.Font.SourceSans
Templatebbc.Text = "Press \";\" to toggle GUI & \"]\" to toggle gold name."
Templatebbc.TextColor3 = Color3.fromRGB(0, 0, 0)
Templatebbc.TextScaled = true
Templatebbc.TextSize = 14.000
Templatebbc.TextWrapped = true

Folder.Parent = _7083G2222

Template.Name = "Template"
Template.Parent = _7083G2222
Template.BackgroundColor3 = Color3.fromRGB(115, 115, 115)
Template.BorderSizePixel = 0
Template.Size = UDim2.new(0, 397, 0, 22)
Template.Visible = false
Template.Font = Enum.Font.SourceSans
Template.Text = ""
Template.TextColor3 = Color3.fromRGB(255, 255, 255)
Template.TextScaled = true
Template.TextSize = 14.000
Template.TextStrokeTransparency = 0.000
Template.TextWrapped = true

Viewing.Name = "Viewing"
Viewing.Parent = Template

-- Scripts:

local function NJHGMYJ_fake_script() -- MainFrame.Drag 
	local script = Instance.new('LocalScript', MainFrame)

	local UserInputService = game:GetService("UserInputService")
	local runService = (game:GetService("RunService"));
	
	local gui = script.Parent
	
	local dragging
	local dragInput
	local dragStart
	local startPos
	
	function Lerp(a, b, m)
		return a + (b - a) * m
	end;
	
	local lastMousePos
	local lastGoalPos
	local DRAG_SPEED = (8); -- // The speed of the UI darg.
	function Update(dt)
		if not (startPos) then return end;
		if not (dragging) and (lastGoalPos) then
			gui.Position = UDim2.new(startPos.X.Scale, Lerp(gui.Position.X.Offset, lastGoalPos.X.Offset, dt * DRAG_SPEED), startPos.Y.Scale, Lerp(gui.Position.Y.Offset, lastGoalPos.Y.Offset, dt * DRAG_SPEED))
			return 
		end;
	
		local delta = (lastMousePos - UserInputService:GetMouseLocation())
		local xGoal = (startPos.X.Offset - delta.X);
		local yGoal = (startPos.Y.Offset - delta.Y);
		lastGoalPos = UDim2.new(startPos.X.Scale, xGoal, startPos.Y.Scale, yGoal)
		gui.Position = UDim2.new(startPos.X.Scale, Lerp(gui.Position.X.Offset, xGoal, dt * DRAG_SPEED), startPos.Y.Scale, Lerp(gui.Position.Y.Offset, yGoal, dt * DRAG_SPEED))
	end;
	
	gui.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = gui.Position
			lastMousePos = UserInputService:GetMouseLocation()
	
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
				end
			end)
		end
	end)
	
	gui.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)
	
	runService.Heartbeat:Connect(Update)
end
coroutine.wrap(NJHGMYJ_fake_script)()
local function GWAASU_fake_script() -- Folder.LocalScript 
	local script = Instance.new('LocalScript', Folder)

	local ScreenGui = script.Parent.Parent.MainFrame
	ScreenGui.DescendantAdded:Connect(function(Descendant)
		if Descendant:IsA("TextButton") or Descendant:IsA("ImageButton") then
			Descendant.MouseButton1Click:Connect(function()
				local EnemyPlayer = game.Players[Descendant.PlayerName.Value]
				local Player = game.Players.LocalPlayer
				for i,v in pairs(ScreenGui:GetChildren()) do
					if v.Name ~= Descendant.Name and v:FindFirstChild("Viewing") then
						v.Viewing.Value = false
						v.TextColor3 = Color3.new(255,255,255)
					end
				end
				if Descendant.Viewing.Value == false then
					Descendant.Viewing.Value = true
					Descendant.TextColor3 = Color3.new(0,0,0)
					game.Workspace.Camera.CameraSubject = EnemyPlayer.Character.Humanoid
					return end
				if Descendant.Viewing.Value == true then
					Descendant.Viewing.Value = false
					Descendant.TextColor3 = Color3.new(255,255,255)
					game.Workspace.Camera.CameraSubject = Player.Character.Humanoid
					return end
			end)
		end
	end)
end
coroutine.wrap(GWAASU_fake_script)()
local function MBNKX_fake_script() -- _7083G2222.Main 
	local script = Instance.new('LocalScript', _7083G2222)

	script.Parent.Name = ""..math.random(1,10000).."G"..math.random(1,4000)
	
	repeat wait() until game.Players.LocalPlayer.PlayerGui:FindFirstChild("LeaderboardGui")
	
	for i,v in pairs(game.Players:GetChildren()) do
	
	repeat wait() until v:FindFirstChild("leaderstats")
	repeat wait() until v.leaderstats:FindFirstChild("Lord")
	
		local Gui = script.Parent.Template:Clone()
		Gui.Name = v.Name
		Gui.Text = "("..v.Name..") "..v:WaitForChild("leaderstats").Lord.Value.." "..v:WaitForChild("leaderstats").FirstName.Value.." "..v:WaitForChild("leaderstats").LastName.Value.." "..v:WaitForChild("leaderstats").UberTitle.Value
		Gui.Parent = script.Parent.MainFrame
		Gui.Visible = true
		local Original = Instance.new("StringValue")
		Original.Parent = Gui
		Original.Name = "PlayerName"
		Original.Value = v.Name
	
	end
	--game.Workspace.Camera.CameraSubject = v.Character.Humanoid
	game.Players.PlayerAdded:Connect(function(Player)
	
		local Gui = script.Parent.Template:Clone()
		Gui.Name = Player.Name
		Gui.Text = "("..Player.Name..") "..Player:WaitForChild("leaderstats").Lord.Value.." "..Player:WaitForChild("leaderstats").FirstName.Value.." "..Player:WaitForChild("leaderstats").LastName.Value.." "..Player:WaitForChild("leaderstats").UberTitle.Value
		Gui.Parent = script.Parent.MainFrame
		Gui.Visible = true
		local Original = Instance.new("StringValue")
		Original.Parent = Gui
		Original.Name = "PlayerName"
		Original.Value = Player.Name
	
	end)
	
	game.Players.PlayerRemoving:Connect(function(Player)
	
		for i,v in pairs(script.Parent.MainFrame:GetChildren()) do
	
			if v.Name == Player.Name then
				v:Destroy()
			end
	
		end
	
	end)
end
coroutine.wrap(MBNKX_fake_script)()
local function WFSM_fake_script() -- _7083G2222.Warning 
	local script = Instance.new('LocalScript', _7083G2222)

	local Thing = script.Parent.Templatebbc:Clone()
	Thing.Visible = true
	Thing.Parent = script.Parent.SecondaryFrame
	game.Debris:AddItem(Thing,8)
	
	local Off = false
	
	local User = game:GetService("UserInputService")
	
	User.InputBegan:Connect(function(Input)
	
		if Input.KeyCode == Enum.KeyCode.Semicolon then
	
			if Off == false then
				Off = true
				script.Parent.MainFrame.Visible = false
				return end
	
			if Off == true then
				Off = false
				script.Parent.MainFrame.Visible = true
				return end
	
	
	
		end
	
	end)
end
coroutine.wrap(WFSM_fake_script)()
local function XYLI_fake_script() -- _7083G2222.ViewHP 
	local script = Instance.new('LocalScript', _7083G2222)

	while true do
	
	local Live = workspace.Live
	for i,v in pairs(Live:GetChildren()) do
		if v:FindFirstChildOfClass("Humanoid") then
			v.Humanoid.HealthDisplayType = Enum.HumanoidHealthDisplayType.AlwaysOn
			v.Humanoid.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.Subject
			v.Humanoid.HealthDisplayDistance = 100
		end
	end
	
	Live.ChildAdded:Connect(function(added)
	
		if added:FindFirstChildOfClass("Humanoid") then
			added.Humanoid.HealthDisplayType = Enum.HumanoidHealthDisplayType.AlwaysOn
			added.Humanoid.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.Subject
			added.Humanoid.HealthDisplayDistance = 100
		end
	
		end)
		
		task.wait(5)
		
		end
end
coroutine.wrap(XYLI_fake_script)()
local function ZWET_fake_script() -- _7083G2222.ToggleGoldName 
	local script = Instance.new('LocalScript', _7083G2222)

	repeat wait() until game.Players.LocalPlayer.PlayerGui:FindFirstChild("LeaderboardGui")
	
	game.Players.LocalPlayer:WaitForChild("leaderstats").MaxEdict.Value = true
	
	local Off = false
	
	game:GetService("UserInputService").InputBegan:Connect(function(Input)
		
		if Input.KeyCode == Enum.KeyCode.RightBracket then
			
			if Off == false then
				
				Off = true
				
				game.Players.LocalPlayer:WaitForChild("leaderstats").MaxEdict.Value = false
				
				return end
			
			if Off == true then
	
				Off = false
	
				game.Players.LocalPlayer:WaitForChild("leaderstats").MaxEdict.Value = true
	
				return end
			
		end
		
	end)
end
coroutine.wrap(ZWET_fake_script)()
